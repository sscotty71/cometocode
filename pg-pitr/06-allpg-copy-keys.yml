
  - name: Copy the backup server keys to the pgbackrest machine
    become: yes
    become_user: root
    authorized_key:
      user: "{{ pg_user }}"
      state: present
      key: "{{ lookup('file','keyspg/{{item}}-id_rsa.pub')}}"
    with_items: 
      - "{{ groups['allpg'] }}"  
